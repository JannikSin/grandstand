{
  "name": "grandstand",
  "version": "1.0.0",
  "private": true,
  "description": "Dev tooling only. The app is zero-build static files; nothing here ships.",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/*.test.js\"",
    "update": "node scripts/update-f1.mjs && node scripts/update-purdue.mjs && node scripts/update-tennis.mjs"
  }
}
